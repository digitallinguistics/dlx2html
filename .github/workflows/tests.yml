name: tests
on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: '00 23 * * SAT'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: npm ci
      - run: npm test